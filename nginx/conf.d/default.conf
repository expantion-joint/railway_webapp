# server {
#     listen 80;
#     # server_name localhost; # 開発環境
#     server_name 35.74.213.11; # 本番環境 EC2のElastic IPアドレス

#     root /src/public; # 仮想環境内のroot
#     index index.html;

# 	client_max_body_size 100M;  # サーバーの容量を増やす（デフォルトは1MB）

#     location / {
#         proxy_pass http://rails-railway:3000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }

#     error_page 500 502 503 504 /50x.html;
#     location = /50x.html {
#         root /usr/share/nginx/html;
#     }
# }

upstream unicorn {
  server unix:/src/tmp/sockets/unicorn.sock; # Docker 内の適切なパスに変更
}

server {
  listen 80;
  server_name oshitetsu.com;
  root /src/public; # Docker コンテナのマウントディレクトリに変更

  client_max_body_size 100M;  # サーバーの容量を増やす（デフォルトは1MB）

  location ^~ /assets/ {
    gzip_static on;
    expires max;
    add_header Cache-Control public;
  }

  location @unicorn {
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header Host $http_host;
    proxy_redirect off;
    proxy_pass http://unicorn;
  }

  try_files $uri/index.html $uri @unicorn;
}
